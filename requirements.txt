openai>=1.0
pandas>=2.0
pyarrow>=14.0
pytest>=8.0
scikit-learn>=1.4
joblib>=1.3

# Router featurizer: a frozen embedding model (verl_code_rl/embedding.py),
# not TF-IDF. Already present if you're using the same venv as verl/vLLM;
# listed explicitly since this file is also meant for a standalone
# orchestration env.
torch>=2.0
transformers>=4.45

# For verl_code_rl/extract_sft_lora_adapter.py (reconstructs a loadable LoRA
# adapter from a verl native SFT checkpoint).
peft>=0.10

# Install verl separately in the training environment, matching your CUDA stack:
#   pip install -e /path/to/verl
# or follow https://verl.readthedocs.io/ installation docs.
